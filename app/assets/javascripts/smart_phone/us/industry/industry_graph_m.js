// Generated by CoffeeScript 1.6.3
(function() {
  $(function() {
    var drawChart;
    drawChart = function(values, basis) {
      var barChartData, scores,
        _this = this;
      scores = [];
      _.each(values, function(value) {
        return scores.push(value["" + basis] * 100 / 1000000);
      });
      barChartData = {
        labels: ['1', '2', '3', '4', '5'],
        datasets: [
          {
            fillColor: "#1a243f",
            strokeColor: "#fff",
            data: scores
          }
        ]
      };
      return PS.graph.barChart(document.getElementById("Industry_" + basis).getContext("2d"), barChartData);
    };
    drawChart(gon.average_sale, "sale");
    drawChart(gon.average_operating_profit, "operating_profit");
    drawChart(gon.average_net_income, "net_income");
    return $(".js-barchart-unit").text("（百万円）");
  });

}).call(this);
