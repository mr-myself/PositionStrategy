// Generated by CoffeeScript 1.6.3
$(function() {
  var getAchievements;
  getAchievements = function(basis) {
    var labels, lineChartData, scores,
      _this = this;
    labels = [];
    scores = [];
    _.each(gon.achievement, function(value) {
      labels.push(value.publish_day);
      return scores.push(value["" + basis]);
    });
    lineChartData = {
      labels: labels,
      datasets: [
        {
          fillColor: "rgba(0,0,0,0)",
          strokeColor: "1a243f",
          pointColor: "#1a243f",
          pointStrokeColor: "#fff",
          data: scores
        }
      ]
    };
    return PS.graph.lineChart(document.getElementById("Company_" + basis).getContext("2d"), lineChartData);
  };
  getAchievements("sale");
  getAchievements("operating_profit");
  getAchievements("ordinary_profit");
  return getAchievements("net_income");
});
